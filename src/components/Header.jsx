import React from 'react';

const Header = () => {
    return (
        <header>
            <h1>Timbu Cloud Shop</h1>
            <nav>
                <a href="/">Home</a>
                <a href="/checkout">Checkout</a>
            </nav>
        </header>
    );
};

export default Header